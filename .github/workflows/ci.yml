name: CI

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  test:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    env:
      RAILS_ENV: test
      DATABASE_URL: postgres://postgres:postgres@localhost:5432/readapt_test
      BUNDLE_FROZEN: "false"

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Set up Ruby
        uses: ruby/setup-ruby@v1
        with:
          bundler-cache: false

      - name: Update secured dependencies
        run: >-
          bundle update rails concurrent-ruby puma nokogiri rails-html-sanitizer
          websocket-driver net-imap crass devise faraday json jwt mail msgpack
          brakeman --conservative

      - name: Prepare database
        run: bin/rails db:prepare

      - name: Run tests
        run: bin/rails test

  quality:
    runs-on: ubuntu-latest
    env:
      BUNDLE_FROZEN: "false"

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Set up Ruby
        uses: ruby/setup-ruby@v1
        with:
          bundler-cache: false

      - name: Update secured dependencies
        run: >-
          bundle update rails concurrent-ruby puma nokogiri rails-html-sanitizer
          websocket-driver net-imap crass devise faraday json jwt mail msgpack
          brakeman --conservative

      - name: Run RuboCop
        run: bundle exec rubocop app lib Gemfile Rakefile

      - name: Run Brakeman
        run: bundle exec brakeman --no-pager

      - name: Verify latest Brakeman
        run: gem install brakeman --no-document && brakeman --ensure-latest --no-pager

      - name: Run Bundler Audit
        run: bundle exec bundler-audit check --update
