<div class="container">
  <%= simple_form_for @text, url: texts_path do |f| %>
    <%= f.input :title %>
    <%= f.input :content %>
    <%= f.button :submit, "Créer", class: "btn btn-success"%>
  <% end %>
</div>
