<div class="container">
  <%= simple_form_for @text, url: texts_path do |f| %>
    <%= f.input :title %>
    <%= f.input :document, as: :file %>
    <%= f.button :submit, "Importer", class: "btn btn-success"%>
  <% end %>
</div>
