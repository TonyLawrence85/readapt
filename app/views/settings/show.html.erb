<div class="px-3">

  <div class="form-hero">
    <div class="sec-eye">Paramètres</div>
    <div class="sec-title">Mes <em>préférences</em></div>
  </div>

  <%= render "show_content", setting: @setting %>

  <%= link_to edit_setting_path(@setting), class: "btn btn-readapt w-100" do %>
    <i class="fa-solid fa-pen-to-square me-1"></i> Modifier mes préférences
  <% end %>

</div>
